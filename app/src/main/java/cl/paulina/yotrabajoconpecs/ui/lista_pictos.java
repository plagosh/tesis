package cl.paulina.yotrabajoconpecs.ui;

import androidx.fragment.app.Fragment;

public class lista_pictos extends Fragment {

/*    diccionario dicc;
    int j;
    ListView list;
    ArrayList<String> palabras = new ArrayList<String>();

    public View onCreateView(@NonNull LayoutInflater inflater,
                             ViewGroup container, Bundle savedInstanceState) {

        View vista = inflater.inflate(R.layout.lista_pictogramas, container, false);
        dicc.actualizarDiccionario(palabras, j);
        list = vista.findViewById(R.id.listaview);
        ArrayAdapter<String> adapterlista = new ArrayAdapter<>(this, android.R.layout.list_content, palabras);
        list.setAdapter(adapterlista);
    }

 */
}